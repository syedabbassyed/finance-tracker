import { MenuKey } from "./iconMappings";

const MENU_LABELS: Record<MenuKey, string> = {
    bills: "Bills",
    goals: "Goals",
    logout: "Logout",
    overview: "Overview",
    transactions: "Transactions"
};

export default MENU_LABELS;